# Миграции базы данных

Миграции схемы PostgreSQL управляются Alembic.

Источник конфигурации подключения — переменная окружения `DATABASE_URL`.
Секреты подключения не хранятся в `alembic.ini` и не коммитятся в Git.
